# craftworks-sdk

App-facing SDK. Every substrate capability is exposed here the phase it lands:
`collection` (1) · `identity` `query` `subscribe` (3) · `inbox` `edge` `stream` (6) ·
`cap` (8) · `file` `blob` (9) · `ledger` (10).
