[package]
name = "testkit"
version = "0.0.0"
edition = "2021"
publish = false
description = "The probed fixtures. The only supported way to build the thing under test."

[dependencies]
engine = { path = "../engine" }
protocol = { path = "../protocol" }
engine-delegate = { path = "../engine-delegate" }
craftworks-sdk = { path = ".." }
freenet-prolly = { git = "https://github.com/craft-ec/freenet-prolly", rev = "9764b86" }
instrument = { git = "https://github.com/craft-ec/craftworks-instrument", rev = "4045d3c" }

[dev-dependencies]
instrument = { git = "https://github.com/craft-ec/craftworks-instrument", rev = "4045d3c" }
